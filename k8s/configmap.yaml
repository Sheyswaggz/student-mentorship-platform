apiVersion: v1
kind: ConfigMap
metadata:
  name: mentorship-platform-config
  namespace: development
  labels:
    app: mentorship-platform
    environment: development
    managed-by: kubernetes
data:
  LOG_LEVEL: "info"
  ENVIRONMENT: "development"
  NODE_ENV: "production"
  NEXTAUTH_URL: "http://localhost:3000"
  PORT: "3000"
  HOSTNAME: "0.0.0.0"